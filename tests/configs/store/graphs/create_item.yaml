nodes:
  validate-item:
    endpoint-id: store/validate-item
    dependencies:
      - node-id: input
        item-id: http_request
        overriden-item-id: request
  save-item:
    endpoint-id: store/save-item
    dependencies:
      - node-id: validate-item
        item-id: name
      - node-id: validate-item
        item-id: price

output-node: save-item
