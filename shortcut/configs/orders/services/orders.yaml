host: localhost:8081
endpoints:
    get_orders:
        path: shortcut/get-orders
        timeout_ms: 5000
        retries_num: 2
        result_ids:
            - get_all_orders_result
    post_orders:
        path: shortcut/post-orders
        result_ids:
            - error
